The file resources.xml should be placed in this folder. It is excluded for security purposes.

It should have the following form:

<?xml version="1.0" encoding="utf-8" ?>
<resources>
    <Resource id="partyDataSource" type="javax.sql.DataSource">
        jdbcDriver = org.postgresql.Driver
        jdbcUrl = jdbc:postgresql://data1.hib.no/{database}
        userName = {username}
        password = {password}
    </Resource>
</resources>

Replace the {} templates with real values.

FontAwesome CDN embed code:

<script src="https://use.fontawesome.com/a273eb4620.js"></script>
